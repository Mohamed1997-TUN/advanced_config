active_test: "compliant_test"

compliant_test:
  policy_name: "Allow HTTPS"
  srcintf: "port2"
  dstintf: "port1"
  service: "HTTPS"  # This matches a rule in the matrix
  policy_action: "accept"
  srcaddr: "all"
  dstaddr: "all"
  schedule: "always"
  nat: "enable"
